_target_: src.data.DataModule
dataset: sunrgbd
batch_size: ${batch_size}
num_workers: ${num_workers}
pin_memory: true
args:
  augment: true
  data_dir: ${paths.data_dir}/SUNRGBD
  num_sample: ${num_sample}
  radio: 0.2